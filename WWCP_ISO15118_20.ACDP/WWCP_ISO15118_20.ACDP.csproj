<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>preview</LangVersion>
    <RootNamespace>cloud.charging.open.protocols.ISO15118_20.ACDP</RootNamespace>
    <AssemblyName>WWCP_ISO15118_20.ACDP</AssemblyName>
    <IsAotCompatible>true</IsAotCompatible>

    <!-- The generator reads these to name the generated codec, so it does not collide with the
         CommonMessages/DC/AC/WPT/-2/AppProtocol codecs (all from the same source generator). -->
    <ExiGeneratedNamespace>cloud.charging.open.protocols.ISO15118_20.ACDP.Generated</ExiGeneratedNamespace>
    <ExiCodecClassName>AcdpCodec</ExiCodecClassName>

    <!-- Write the generated codec to obj/ for inspection. -->
    <EmitCompilerGeneratedFiles>true</EmitCompilerGeneratedFiles>
  </PropertyGroup>

  <ItemGroup>
    <CompilerVisibleProperty Include="ExiGeneratedNamespace" />
    <CompilerVisibleProperty Include="ExiCodecClassName" />
    <CompilerVisibleProperty Include="ExiFragmentElements" />
    <CompilerVisibleProperty Include="ExiDocumentElementOrder" />
    <CompilerVisibleProperty Include="ExiParticleGrammar" />
  </ItemGroup>

  <ItemGroup>
    <!-- ACDP + the CommonTypes/xmldsig it imports, as one collected set. CommonTypes is deliberately
         duplicated per message-set assembly (mirrors cbexigen/cbV2G itself). Fetched from
         https://standards.iso.org/iso/15118/-20/ed-1/en/ (same source as the other -20 XSDs in this
         repo; see docs/xsd-inventory-15118-20.md). -->
    <AdditionalFiles Include="Schemas\V2G_CI_ACDP.xsd" />
    <AdditionalFiles Include="Schemas\V2G_CI_CommonTypes.xsd" />
    <AdditionalFiles Include="Schemas\xmldsig-core-schema.xsd" />
  </ItemGroup>

  <ItemGroup>
    <!-- The generated codec uses BitWriter / BitReader / ExiPrimitives from the prototype. -->
    <ProjectReference Include="..\WWCP_ISO15118_EXI\WWCP_ISO15118_EXI.csproj" />
    <ProjectReference Include="..\WWCP_ISO15118_EXI_SourceGenerator\WWCP_ISO15118_EXI_SourceGenerator.csproj"
                      OutputItemType="Analyzer"
                      ReferenceOutputAssembly="false" />
  </ItemGroup>
</Project>

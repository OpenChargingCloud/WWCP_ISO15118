<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>preview</LangVersion>
    <RootNamespace>cloud.charging.open.protocols.ISO15118.Cli</RootNamespace>
    <AssemblyName>WWCP_ISO15118_CLI</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="ISO15118ConformanceTests.Simulation" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\WWCP_ISO15118_Session\WWCP_ISO15118_Session.csproj" />
    <!-- The BouncyCastle TLS backend option generates a strict-20 V2G PKI at runtime and shares it
         via a pki-dir between the two dev processes. -->
    <ProjectReference Include="..\WWCP_ISO15118_PKI\WWCP_ISO15118_PKIBuilder\WWCP_ISO15118_PKIBuilder.csproj" />
  </ItemGroup>
</Project>
